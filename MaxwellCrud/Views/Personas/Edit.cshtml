@model MaxwellCrud.Models.Persona

@{
    ViewData["Title"] = "Edit";
}

<h1>Editar</h1>

<h4>Persona</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="PersonaId" />
            <div class="form-group">
                <label asp-for="PersonaNombre" class="control-label"></label>
                <input asp-for="PersonaNombre" class="form-control" />
                <span asp-validation-for="PersonaNombre" class="text-danger"></span>
            </div>

            <div class="form-group">
                <input type="submit" value="Guardar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" class="btn btn-info">Cancelar acción</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
