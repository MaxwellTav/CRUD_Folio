@model MaxwellCrud.Models.Persona

@{
    ViewData["Title"] = "Details";
}

<h1>Detalles</h1>

<div>
    <h4>Persona</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.PersonaNombre)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.PersonaNombre)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.PersonaDate)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.PersonaDate)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.PersonaId" class="btn btn-warning">Editar</a> |
    <a asp-action="Index" class="btn btn-info">Volver a la lista</a>
</div>
